# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.3.4

# ── ML / AI ───────────────────────────────────────────────────────────────────
torch==2.3.1               # CPU build by default; swap for torch+cu121 on GPU nodes
torchvision==0.18.1
transformers==4.42.4       # HuggingFace — hate speech + spoiler classifiers
Pillow==10.4.0             # Image loading for NSFW frame analysis
numpy==1.26.4
scikit-learn==1.5.1        # Threshold calibration utilities
huggingface-hub==0.23.4    # Model download/cache

# ── HTTP client (for fetching frames from R2 presigned URLs) ─────────────────
httpx==0.27.0

# ── Observability ─────────────────────────────────────────────────────────────
structlog==24.2.0
prometheus-fastapi-instrumentator==7.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-multipart==0.0.9
tenacity==8.5.0            # retry logic for model inference