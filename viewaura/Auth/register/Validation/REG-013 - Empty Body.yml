info:
  name: Register - Empty Body
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        expect(res.status).to.equal(400);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Test ID: REG-013
  Category: Negative
  Description: Failed registration with completely empty JSON body
  Expected: 400 Bad Request