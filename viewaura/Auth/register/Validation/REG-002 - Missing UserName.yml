info:
  name: Register - Missing Username
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "david.solemn@gmail.com",
        "password": "Password1@",
        "display_name": "Solemnseagal",
        "first_name": "David",
        "last_name": "Solemn"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        expect(res.status).to.equal(400);
        expect(res.body.message).to.include("user_name");

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Test ID: REG-002
  Category: Negative
  Description: Failed registration with user_name field absent
  Expected: 400 Bad Request, error references "user_name"