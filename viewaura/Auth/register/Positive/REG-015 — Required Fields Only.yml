info:
  name: Register - Required Fields Only
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "minimal.user@gmail.com",
        "password": "Password1@",
        "display_name": "MinimalUser",
        "first_name": "Minimal",
        "last_name": "User",
        "user_name": "min_user"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        expect(res.status).to.equal(201);
        expect(res.body).to.have.property("data");

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Test ID: REG-015
  Category: Positive
  Description: Successful registration with only required fields; optional fields omitted
  Expected: 201 Created, returns user data